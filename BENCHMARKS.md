# 実測結果

記事に載せている数字の正本。すべてこのリポジトリで実際に走らせたもの。

**測定環境**

```
goos: darwin
goarch: arm64
pkg: github.com/makoto-developer/category-theory-go
cpu: Apple M5 Pro (18 threads)
go version go1.25.1 darwin/arm64
```

再現コマンド:

```bash
go test -bench=. -benchmem -count=6 -run='^$' ./part1/
```

数値は `-count=6` の代表値（外れ値を除いた中央付近）。

---

## part1 — 合成のコスト

### 合成の組み方による差（int → int の軽い射）

| 書き方 | ns/op | B/op | allocs/op | 手書きとの差 |
|---|---:|---:|---:|---:|
| 手書き `increment(double(x))` | 1.57 | 0 | 0 | — |
| `Compose` 2段・変数に保存して呼ぶ | 1.73 | 0 | 0 | +0.16 ns |
| `Compose` 2段・呼ぶたびにその場で合成 | 12.7 | 32 | 1 | **+11.1 ns** |
| 手書き3段 `negate(increment(double(x)))` | 1.54 | 0 | 0 | — |
| `Compose` 3段・変数に保存（静的な入れ子） | 2.50 | 0 | 0 | +0.96 ns |
| `Pipe` 3段（スライス経由で組む） | 3.90 | 0 | 0 | +2.36 ns |
| 合成そのものの構築コスト（`Compose` 2回） | 22.0 | 64 | 2 | — |

読み取れること:

- **合成を変数に保存して使い回す限り、2段の合成コストは手書きとの差が 1 ns 未満。実質ゼロ**。コンパイラが関数値の呼び出しを潰している
- **呼ぶたびに合成し直すと 7倍以上遅くなる**（1.73 → 12.7 ns。手書き 1.57 ns と比べれば8倍）。クロージャがヒープに逃げ、32 B のアロケーションが毎回発生する。合成はハンドラ構築時に済ませ、リクエストごとに組み直さないこと
- 同じ3段でも、`Compose` の静的な入れ子（2.50 ns）よりスライス経由の `Pipe`（3.90 ns）が遅い。スライスの中身はコンパイル時に分からないため、各段が本物の間接呼び出しになる

> **但し書き（2026-08-13）**: 上の表のうち **1 ns 級の差（`+0.16 ns`、`+0.96 ns`）は再現しません。** 同じ環境で測り直すと `ComposedStored` は 1.82〜1.90 ns の run と 2.47〜2.77 ns の run があり、手書きとの差は +0.07〜+0.8 ns まで振れます。この粒度はコードのアラインメントや分岐予測の状態で変わるので、`-count=6` の代表値では詰められません。**この表で信用してよいのは、桁が違う行（12.7 ns、22.0 ns）と、下の「段数を増やしたときの傾き」のように差が積み上がる測り方だけ**です。1 ns 級を主張するなら `benchstat` で変動幅ごと出す必要があります。

### 合成の段数を増やしたときの傾き

| 段数 | `Pipe` で合成 (ns/op) | ループで直接呼ぶ (ns/op) |
|---:|---:|---:|
| 1 | 2.02 | 1.62 |
| 2 | 2.95 | 1.58 |
| 4 | 4.82 | 2.72 |
| 8 | 8.78 | 4.85 |
| 16 | 18.4 | 8.25 |

- 合成 1段あたり **約 1.09 ns**（(18.4 − 2.02) / 15）
- ループで直接呼ぶ場合は 1段あたり **約 0.44 ns**
- 差の **約 0.65 ns/段** が、クロージャを1枚挟むことの代金

### 内側の処理が重い場合

| 書き方 | ns/op | B/op | allocs/op |
|---|---:|---:|---:|
| 手書き `isEven(length(strconv.Itoa(x)))` | 11.9 | 8 | 1 |
| 同じ処理を関数値経由で呼ぶ | 12.2 | 8 | 1 |
| `Compose` 3段・変数に保存 | 13.4 | 8 | 1 |

`strconv.Itoa` が 1 回アロケートする（8 B）ため、そちらが支配的になる。合成のコストは全体の約 10%（+1.5 ns）に薄まる。

**判断基準**: 射1本の中身が 10 ns 以上（文字列変換1回、mutex 1回、map アクセス数回）なら合成のコストは誤差。1 ns 級の算術だけを繋ぐホットループでは、合成は 1.5〜2.5 倍の差になる。

### インライン化の判定

```bash
go test -gcflags='-m' -run='^$' -bench='^$' ./part1/ 2>&1 | grep compose.go
```

```
part1/compose.go:7:6: can inline Compose[go.shape.int,go.shape.string,go.shape.int]
part1/compose.go:8:9: can inline Compose[go.shape.int,go.shape.string,go.shape.int].func1
part1/compose.go:12:6: can inline Identity[go.shape.int]
part1/compose.go:16:6: can inline Pipe[go.shape.int]
```

`Compose` も、それが返すクロージャも「インライン化可能」と判定されている。ジェネリック関数は `go.shape.*`（同じメモリ表現を持つ型をまとめた形）単位でインスタンス化されるため、`Compose[int,string,int]` と `Compose[int,int,bool]` は別々に現れる。

---

## part2 — 法則が許可する最適化

### Functor 則2（合成の保存）＝ ループ融合の許可証

`MapSlice(MapSlice(xs, f), g)` と `MapSlice(xs, Compose(f, g))` は法則により等しい。等しいなら速いほうを選んでよい。

| 要素数 | 2回writeする (ns/op) | 融合 (ns/op) | 2回 (B/op) | 融合 (B/op) |
|---:|---:|---:|---:|---:|
| 100 | 570 | 321 | 2,688 | 928 |
| 10,000 | 127,000 | 111,000 | 284,643 | 120,833 |
| 1,000,000 | 12,430,000 | 11,850,000 | 31,729,827 | 15,722,508 |

- 小さい配列では **1.8倍速**、メモリは **1/3**
- 大きい配列でも **メモリは半分**（中間スライスが消えるため）。速度差は 5% 程度に縮む

### モノイド則＝実装差し替えの許可証

`strings.Builder` も `strings.Join` も、文字列連結という同じモノイドの別実装である。結合律があるので、どうまとめても答えは同じ。だから最も速い実装に置き換えてよい。

1,000 個の 8 バイト文字列を連結:

| 実装 | ns/op | B/op | allocs/op | 最速比 |
|---|---:|---:|---:|---:|
| `a + b` で畳み込む | 228,000 | 4,273,899 | 999 | 55× |
| 毎回 `strings.Builder` を作って連結 | 238,000 | 4,273,910 | 1,000 | 58× |
| `strings.Builder` を1つ使い回す | 4,480 | 34,296 | 15 | 1.1× |
| `strings.Join` | 4,120 | 8,192 | 1 | **1×** |

同じ法則を満たす実装の間に **55倍** の開きがある。法則は「置き換えてよい」とは言うが「同じ速さだ」とは言わない。

### 結合律＝並列化の許可証（ただし損益分岐点がある）

`int` の加算を、逐次と18分割並列で畳み込む。

| 要素数 | 逐次 (ns/op) | 並列 (ns/op) | 並列/逐次 |
|---:|---:|---:|---:|
| 1,000 | 622 | 5,050 | **8.1× 遅い** |
| 10,000 | 5,650 | 8,500 | 1.5× 遅い |
| 30,000 | 17,800 | 15,100 | 1.18× 速い |
| 100,000 | 63,200 | 31,100 | 2.0× 速い |
| 10,000,000 | 6,320,000 | 1,230,000 | **5.1× 速い** |

- 損益分岐点は **2〜3万要素**（加算のような 1 命令の演算の場合）
- 並列版は常に 2,752 B / 38 allocs の固定コストを払う
- 結合律は並列化を **可能にする** だけで、**得になる** ことは保証しない

### middleware チェーンのコスト

| 段数 | ns/op | B/op | allocs/op |
|---:|---:|---:|---:|
| 0（素通し） | 1.73 | 0 | 0 |
| 1 | 2.9 | 0 | 0 |
| 4 | 6.8 | 0 | 0 |
| 16 | 57.9 | 0 | 0 |

- 4段までは 1段あたり約 1.3 ns。16段では 1段あたり約 3.5 ns と単価が上がる（間接呼び出しの連鎖が分岐予測に乗らなくなる）
- 実行時のアロケーションはゼロ。チェーンの構築（4段）が 61.3 ns / 128 B / 5 allocs なので、**起動時に1回組めば、リクエスト処理中の追加コストは 7 ns 程度**

### 平均は並列化で壊れる

```
逐次 = 5.5 / 3分割 = 6.166666666666667 （差 = 0.667）
```

`[1..10]` の平均を3分割して部分平均の平均を取ると、答えが変わる。平均は結合的でないためモノイドにならない。合計と個数の組（`SumCount`）に分ければ両方とも結合的になり、並列化しても一致する。

### 浮動小数点の加算は結合的でない

```
a, b, c := 1e16, -1e16, 1.0
(a+b)+c = 1 / a+(b+c) = 0
```

`float64` の加算はモノイドの法則を満たさない。分割数を変えると集計結果が変わりうる。

---

## part3 — 抽象化の値段

### F代数を経由する fold と、手書き再帰

深さ12の完全二分木（4,095 節点）を評価:

| 方式 | ns/op | B/op | allocs/op |
|---|---:|---:|---:|
| `Fold` + `EvalAlgebra`（代数を差し替え可能） | 26,700 | 0 | 0 |
| 手書きの再帰 `EvalDirect` | 16,500 | 0 | 0 |

代数（関数値の集まり）を経由する分、**1.6倍遅い**。アロケーションはどちらもゼロ。柔軟性の代金として 1.6倍を払うかどうかが判断になる。

### 代数の積 —— 木を1回だけたどる

| 方式 | ns/op |
|---|---:|
| 2回たどる（評価 → 節点数） | 48,700 |
| `ProductAlgebra` で1回たどる | 38,800 |

**1.26倍速い**。2回を1回にしても 2倍にならないのは、`Pair` の組み立てと2つの代数の呼び出しが残るため。木が大きくてキャッシュに乗らないときほど差が開く。

### `iter.Seq`（push 型）と `iter.Pull`（pull 型）

同じ列を合計する:

| 要素数 | スライス直走査 | push (`range` over `iter.Seq`) | pull (`iter.Pull`) |
|---:|---:|---:|---:|
| 10 | 3.7 ns | 45.6 ns（12×） | 462 ns（**125×**） |
| 1,000 | 246 ns | 1,160 ns（4.7×） | 33,600 ns（**136×**） |
| 100,000 | 27,400 ns | 127,800 ns（4.7×） | 3,340,000 ns（**122×**） |

要素あたりに直すと、スライス 0.27 ns、push 1.28 ns、pull **33 ns**。

- push 型は要素あたり約 1 ns の追加。実用上は問題にならない
- pull 型は**2桁遅い**。`iter.Pull` は内部でコルーチンを作り、1要素ごとに制御を往復させるため
- push と pull は理論的には相互変換できるが、**実行コストは push 比で 26倍違う**（表の倍率はスライス直走査を基準にしているので 120倍。比較の基準を取り違えないこと）

`iter.Pull` は「2本の列をマージする」「先読みが必要」といった、pull でなければ書けない場面に限るべきである。

### 高階カインドが無いことの回避策3種

1,000 要素の `[]int` を `[]string` に写す:

| 方式 | ns/op | B/op | allocs/op |
|---|---:|---:|---:|
| 手書きループ | 12,600 | 19,264 | 901 |
| ジェネリクス（`MapSliceGeneric`） | 12,500 | 19,264 | 901 |
| 型消去（`any` 経由のインターフェース） | 22,800 | 35,288 | **1,902** |
| 辞書渡し（`FunctorDict`） | 11,800 | 19,264 | 901 |

- **ジェネリクスと辞書渡しは手書きループと同じコスト**。抽象化のペナルティはゼロ
- **型消去は 1.8倍遅く、アロケーションが 2倍**。`any` への箱詰め（boxing）が要素ごとに発生する

高階カインドの代用として `any` を選ぶ理由は、性能の面では無い。

---

## part4 — ワークフローエンジン：素直な実装との比較

注文処理（在庫確保 → 決済 → 配送手配 → 通知）を2通りで実装し、同じ入力で比較した。外部サービスは失敗しないフェイク。

### 実行コスト

| 実装 | 機能 | ns/op | B/op | allocs/op |
|---|---|---:|---:|---:|
| 素直な手続き型 | リトライ + 計測 | 224 | 0 | 0 |
| 射の合成 | リトライ + 計測 + ステップ名付与 | 345 | 0 | 0 |
| 射の合成 | 上記 + ステップ単位のタイムアウト | 868 | 816 | 12 |
| 射の合成（デコレータ無し） | なし | 66.5 | 0 | 0 |

- **同じ機能で比べると 224 ns → 345 ns、1.54倍**。4ステップなので **1ステップあたり約 30 ns**、アロケーションはゼロ
- 868 ns の版が遅いのは合成のせいではない。**素直な実装が持っていない「ステップ単位のタイムアウト」を足したから**。`context.WithTimeout` が 1回あたり約 174 ns / 272 B / 4 allocs かかり、それを3ステップに掛けている
- デコレータを一切付けない素の合成は 66.5 ns。素直な実装（224 ns）より速いのは、素直な実装が各ステップで `Recorder`（mutex + map）を叩いているため
- 外部サービスの呼び出しは実際にはミリ秒単位なので、345 ns も 868 ns も**全体の 0.1% 未満**

### 組み立てと図の生成

| 操作 | ns/op | B/op | allocs/op |
|---|---:|---:|---:|
| `Compile`（Plan → 実行可能な射） | 213 | 560 | 18 |
| `Mermaid`（Plan → 図） | 750 | 1,273 | 27 |

`Compile` は起動時に1回払えばよい。`Mermaid` は 750 ns なので、ドキュメント生成やデバッグ画面でその場で呼んでも問題ない水準。

### 行数（コメントと空行を除く）

| 内訳 | 行数 |
|---|---:|
| 素直な実装の `Run` 関数（4ステップ） | 71 |
| 合成版の業務ステップ4つ（`Reserve`/`Charge`/`Arrange`/`Notify`） | 39 |
| 合成版の `Plan` 定義 | 8 |
| 合成器とデコレータ（`Then`/`Sequence`/`WithRetry`/`WithTiming`/`WithTimeout`/`WithLabel`/`Recorder`） | 57 |

- 素直な実装は **1ステップあたり約 18行**（リトライ・計測・キャンセル確認が各ステップに繰り返される）
- 合成版は **1ステップあたり約 11行**（業務ロジック 10行 + `Plan` の1行）
- 合成器の 57行が初期投資。差は 1ステップあたり 7行なので、**9ステップ目から行数で得**になる計算（8ステップでは 56行しか浮かず、57行に1行届かない）。ワークフローが複数あれば合成器は共有できるので、回収はさらに早い

### 同じ定義から導けたもの

```
flowchart TD
    start([注文処理])
    n0[在庫確保<br/>最大3回<br/>2s]
    start --> n0
    n1[決済<br/>5s]
    n0 --> n1
    n2[配送手配<br/>最大3回<br/>2s]
    n1 --> n2
    n3[通知<br/>最大3回]
    n2 --> n3
    n3 --> done([完了])
```

```
注文処理
  - 在庫確保: 最大3回 / 2s
  - 決済: リトライなし / 5s
  - 配送手配: 最大3回 / 2s
  - 通知: 最大3回 / 制限なし
```

実行・dry-run・図・設定一覧の4つが、1つの `Plan` から出ている。素直な実装ではこれらを別途書くことになる。

---

## part5 — Cayley 表現とコストモデル

再現コマンド:

```bash
go test -bench=. -benchmem -count=6 -run='^$' ./part5/
```

### 同じ変換が、同じコードで、正反対に効く（n=10,000）

`FoldNaive` / `FoldCayley` はどちらも `Monoid[M]` を受け取るジェネリック関数で、3つのモノイドに対して走るコードは同一。違うのは `Append` のコストがどちら側に付いているかだけ。

| モノイド | `Append` のコスト | 素朴（左結合） | Cayley 表現 | 比 |
|---|---|---:|---:|---:|
| cons リスト（`Concat`） | $O(\lvert 左 \rvert)$ | 1,172,245,874 ns | 684,970 ns | **1,711倍 速い** |
| snoc リスト（`ConcatR`） | $O(\lvert 右 \rvert)$ | 189,493 ns | 903,319,615 ns | **4,767倍 遅い** |
| 文字列（`+`） | $O(\lvert 左 \rvert + \lvert 右 \rvert)$ | 31,791,395 ns | 43,324,062 ns | 1.4倍 遅い |

アロケーション数が裏を取っている。cons の素朴版が **49,995,021**、snoc の Cayley 版が **50,015,006**。**同じ $O(n^2)$ の仕事が反対側へ移っただけ**で、消えてはいない。

### 漸近の確認 —— $n$ を振ったときの allocs/op

1点だけでは漸近を判定できないので、$n$ を 100 / 1,000 / 10,000 と振る。**アロケーション数は決定的なので、時間より強い証拠になる。**

| $n$ | cons 素朴 | cons Cayley | snoc 素朴 | snoc Cayley |
|---:|---:|---:|---:|---:|
| 100 | 4,950 | 301 | 100 | 5,151 |
| 1,000 | 499,500 | 3,001 | 1,000 | 501,501 |
| 10,000 | 49,995,021 | 30,001 | 10,000 | 50,015,006 |

$n$ を10倍したときに、二次側は**100倍**、線形側は**10倍**になる。cons 素朴版は $n(n-1)/2$ と一致し（4,950 / 499,500 / 49,995,000）、`Concat` が毎回 `acc` の全長をコピーしていることの直接の証拠になっている。Cayley 版は $3n+1$。

### 文字列モノイド（n=10,000）

| 方式 | ns/op | B/op | allocs/op |
|---|---:|---:|---:|
| 素朴（`+` の左結合） | 31,791,395 | 428,519,972 | 10,007 |
| Cayley 表現 | 43,324,062 | 429,479,382 | 30,002 |
| `strings.Builder` | 81,266 | 383,738 | 23 |
| `strings.Builder` + `Grow` | 50,124 | 81,920 | 1 |
| `strings.Join` | 77,807 | 81,920 | 1 |

**Cayley 表現はコピー量を1バイトも減らしていない**（428.5MB → 429.5MB）。Go の文字列の `+` は両辺のぶんを払うので、結合を右へ寄せても総コピー量が変わらない。増えたのはクロージャ2個/要素ぶんのアロケーションだけ。

`strings.Builder` の速さの分解: 素朴 → `Builder` が **391倍**、`Builder` → `Grow` 付きが **1.6倍**。支配しているのは事前確保ではなく、$O(n^2) \to O(n)$ にする可変バッファの償却成長で、これは Cayley 表現とは別の仕組み。

### cons リストモノイド（n=10,000）

| 方式 | ns/op | B/op | allocs/op |
|---|---:|---:|---:|
| 素朴（左結合） | 1,172,245,874 | 1,199,882,592 | 49,995,021 |
| Cayley 表現 | 684,970 | 1,040,018 | 30,001 |
| 前に積んで最後に反転 | 559,464 | 480,000 | 20,000 |

Cayley 表現は効くが、**Go で普通に書く形（`PrependReverse`）には 1.2倍負ける**。文字列側では `Builder`+`Grow` に 864倍負ける。

### Go のバージョン差はない

| ベンチ（n=10,000）| 1.23.12 | 1.24.13 | 1.25.13 | 1.26.5 | 1.26.6 |
|---|---:|---:|---:|---:|---:|
| cons Cayley allocs/op | 30,000 | 30,000 | 30,000 | 30,000 | 30,000 |
| snoc 素朴 allocs/op | 10,000 | 10,000 | 10,000 | 10,000 | 10,000 |
| `Builder`+`Grow` allocs/op | 1 | 1 | 1 | 1 | 1 |

**30本すべてのベンチで `allocs/op` が一致した。** `ns/op` は 1.23 比 0.48〜2.09 倍にばらけて傾向がない。この結果はランタイムの最適化ではなくデータ構造のコストモデルの話なので、バージョンに依存しない。

> **測定方法の但し書き**: バージョン横断の測定は [benchsweep](https://github.com/makoto-developer/benchsweep) で行った（docker compose で `cpus` / `cpuset` / `mem` / `GOMAXPROCS` を揃え、1バージョンずつ順番に実行）。この module は `go 1.25.1` を宣言しているため 1.23 ではビルドできないので、**同じ実装を `go 1.23` の単独 module に置き、`b.Loop()` を `for i := 0; i < b.N; i++` に替えた**うえで走らせている。上の表の絶対値（darwin/arm64）とは環境が違う（Linux VM 越しなので遅い）。**比較してよいのはバージョン間だけ**。
>
> `GOMAXPROCS` を固定しているのは、**Go 1.25 からランタイムが cgroup の CPU 帯域制限を見て `GOMAXPROCS` を決める**ため。固定しないと 1.24 以前は VM の全 CPU を、1.25 以降は制限値を見るので、バージョン差と並列度差が混ざる。

---

## part6 — 双対と融合：中間構造は消えるのか

再現コマンド:

```bash
go test -bench=. -benchmem -count=6 -run='^$' ./part6/
```

### hylomorphism —— 1〜n の総和（n=100,000）

| 方式 | ns/op | B/op | allocs/op | 要素あたり |
|---|---:|---:|---:|---:|
| 中間スライスを作る（`HyloVia`） | 596,727 | 4,101,351 | 26 | 5.97 ns |
| 手で融合（`HyloFused`） | 148,764 | 16 | 1 | 1.49 ns |
| `iter.Seq` を挟む（`HyloSeq`） | 211,196 | 16 | 1 | 2.11 ns |
| 手書きループ | 27,296 | 0 | 0 | 0.27 ns |

**Go のコンパイラは deforestation をしない。** 素直に書けば 4.1MB の中間スライスが実際に確保される。手で融合すると 16 B・1 alloc まで落ちる。

### 融合後に残る代金は Go 1.26 で消えた

上の表は go1.25.1 のもの。**融合しても手書きループの 5.5倍遅いまま**で、中間構造は消えたのに代金が残っていた。`Coalgebra` と `step` が関数値なので、要素ごとに間接呼び出しが2回残るため——と結論しかけたが、バージョンを横断して測ると話が変わった。

[benchsweep](https://github.com/makoto-developer/benchsweep) で隔離環境（docker compose・`cpus=4` / `cpuset=0-3` / `GOMAXPROCS=4`）に揃え、`-count=10` で測った n=100,000 の結果:

| | go1.25.13 | go1.26.5 |
|---|---:|---:|
| 融合版 ns/op | 122,938 | **40,060** |
| 融合版 allocs/op | **1** | **0** |
| 手書きループ ns/op | 24,798 | 32,734 |
| **融合版 ÷ 手書きループ** | **4.96倍** | **1.22倍** |
| `iter.Seq` 版 ns/op | 189,624 | 190,409（**1.00倍・変わらず**） |

**Go 1.26 でクロージャの割り当てが消え（1 → 0 allocs）、融合版が 3.07倍速くなった。** 手書きループとの差は 4.96倍から 1.22倍まで縮み、測定範囲も重なる（1.26 の融合版 36,107〜44,386 / 手書き 29,389〜40,165）。エスケープ解析がクロージャをスタックに載せたためと見られる。

**一方 `iter.Seq` 版は 1.26 でも 1.00倍で、まったく改善していない。** 融合の代金は消えたが、継続渡しの往復は消えていない。

**「融合すればゼロコスト」も「融合しても代金は残る」も、バージョンを言わずに書くと誤りになる。**

> ホスト（macOS / Apple M5 Pro / go1.25.1）の 5.5倍と、コンテナの go1.25 の 4.96倍は整合している。ホストの測定が誤っていたわけではなく、**1バージョンしか測っていなかった**のが問題だった。

### マージソート —— 教科書的な hylomorphism（n=100,000）

| 方式 | ns/op | B/op | allocs/op |
|---|---:|---:|---:|
| 木を作ってから畳む（`MergeSortHylo`） | 14,004,434 | 23,657,763 | 299,998 |
| 木を作らない（`MergeSortFused`） | 8,775,459 | 14,057,748 | 99,999 |
| `slices.Sort` | 5,243,436 | 0 | 0 |

木を作ると **1.60倍**遅く、アロケーションは **3.0倍**（$3n$ 対 $n$）。増えた $2n$ が `SplitTree` の節と葉。融合版でも `slices.Sort` の 1.67倍で、14MB を確保する。

**普通に書くマージソートは、hylomorphism を手で融合したもの**にあたる。
